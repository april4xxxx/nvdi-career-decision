# 女皇入朝｜AI 职场心流决策地图·改造记录

## 记录信息

- 日期：2026-07-20
- 当前版本：1.2.0（下文保留 1.1.0 首次 OpenAI 方案，便于追溯）
- 目标：把纯前端模拟原型改造成可部署、可真实对话、可上传知识、可继续驱动任务与珍宝阁成就的黑客松 Demo。
- 原则：保留现有视觉、业务状态和本地演示能力；新增能力以小范围适配层接入。

## 1.2.0 DeepSeek-V4-Pro 迁移

### 迁移原因与依据

- 用户最终提供的是 DeepSeek API Key，并明确选择 DeepSeek-V4-Pro。
- 2026-07-20 核对 DeepSeek 官方文档，正式 API 模型 ID 为 `deepseek-v4-pro`，Base URL 为 `https://api.deepseek.com`。
- 官方 Chat Completions 支持 JSON Output；本项目使用 `response_format: { type: "json_object" }`，并继续在服务端执行原有结构清洗。
- DeepSeek API 没有沿用原 OpenAI Vector Store/File Search 接口，因此知识库不能只替换 Base URL，必须调整数据流。

### 当前架构

```text
用户问题 + 应用状态
        │
        ├─ 公共《女帝职场决策原则》
        └─ 浏览器从用户典籍中检索出的相关片段（最多 4 段）
        │
        ▼
Vercel /api/chat
        │
        ▼
DeepSeek Chat Completions · deepseek-v4-pro · JSON Output
        │
        ▼
服务端结构清洗 → 奏折 → 朱批 → 地图任务 → 金币/精力 → 珍宝阁成就
```

### 知识库迁移

- TXT、Markdown、PDF 仍通过 `/api/knowledge/upload` 真实读取。
- TXT/Markdown 直接提取文字；PDF 使用 `pdf-parse` 提取可选择文字，扫描图片型 PDF 不做 OCR。Vercel Node 运行时通过显式 `@napi-rs/canvas` 导入补充 `DOMMatrix`、`ImageData`、`Path2D`，并确保 Linux 原生依赖进入函数包。
- 首次 1.2.0 线上 PDF 冒烟发现 Vercel Node File Trace 未自动包含 `pdf.worker.mjs`；`vercel.json` 已为上传函数显式加入该文件，修复过程保留在本记录中。
- 每份文件最大 4MB，提取文本最多 60,000 字；服务端提取后立即返回，不建立长期文件存储。
- 文本保存在当前浏览器 `localStorage(nvdi-full-v1).knowledge.documents`，不同访客天然隔离。
- 发起对话前，浏览器根据用户问题对典籍分块并做轻量词法打分，选择最多 4 个片段。
- 服务端再次限制用户知识为最多 6 份输入、合计 18,000 字，再交给 DeepSeek。

### 环境变量兼容

- 推荐名称：`DEEPSEEK_API_KEY`。
- 当前 Vercel 用户已创建变量 `deepseek`；服务端同时识别两者，优先读取标准名称。
- 默认模型：`DEEPSEEK_MODEL || "deepseek-v4-pro"`。
- 原 `APP_SESSION_SECRET`、`OPENAI_VECTOR_STORE_ID` 不再被 1.2.0 代码读取。

### 代码清理

- 删除 `api/_lib/openai.js`、`api/_lib/session.js`、`scripts/seed-knowledge.mjs` 与旧签名令牌测试。
- 新增 `api/_lib/deepseek.js`，统一管理密钥、上游错误和响应文本提取。
- `knowledge/女帝职场决策原则.md` 保留为可维护原稿，精简核心规则随每次请求发送。

### 1.2.0 验证进度

- 生产部署 `dpl_3n5GHQEdT3ZazCGYPdfxNiwjJQNz` 状态 `READY`，固定域名继续为 `https://nvdi-career-decision.vercel.app`。
- 真实 DeepSeek 请求返回 HTTP 200，`meta.model` 为 `deepseek-v4-pro`，生成一份结构完整的主线决策及 4 个任务。
- 测试请求提供《我的汇报复盘》知识片段，模型在 `sources` 中真实返回该典籍名，证明知识上下文已参与决策。
- 生产 Markdown 上传返回 `status: completed`，正确提取 1,505 字且包含“决策流程”。
- 初次 PDF 生产冒烟暴露两项 Vercel 打包问题：缺少 `@napi-rs/canvas` 运行时图形对象、缺少 `pdf.worker.mjs`；两项均已在代码与 `vercel.json` 修复。
- 同一测试 PDF 在本地函数中返回 HTTP 200、`completed`、1,444 个提取字符。
- PDF worker 修复后的生产重部署第一次遇到 Vercel CLI 临时 `fetch failed`，自动重试又被外层安全审批中断；当前代码已就绪，等待用户明确授权后再次部署并复测 PDF。

2026-07-20 公开站点浏览器端到端验收：

- 使用“直臣”提交一条包含时限、延期原因和期望结果的真实测试问题，页面收到 DeepSeek 奏折；回复采用结论先行风格。
- 奏折包含投入、收益、机会成本、推荐路径，并显示来源《女帝职场决策原则》。
- 点击“同意”后，页面真实生成 3 项 `main` 任务并投放朝堂，不是仅展示模型文本。
- 完成其中“梳理延期事实与影响”后，精力从 100 结算为 95，任务标记为“已办”，并解锁“初开窑火”“独任其事”“初见重臣”。
- 以相同场景分别请求“顺臣”和“卦师”，两次均返回 `meta.mode: deepseek`、`meta.model: deepseek-v4-pro`：顺臣先承接焦虑再建议，卦师强调低风险试探与进退机制。
- 测试数据只写入执行验收的浏览器本地存档；其他评委设备不会看到该测试进度。

以下“架构决定”至 1.1.0 验证记录描述首次 OpenAI 方案，是迁移历史，不代表 1.2.0 当前实现。

## 改造前状态

- 项目为无构建步骤的 HTML/CSS/JavaScript 静态站点。
- `data.brain.analyze()` 通过关键词与固定情景模拟对话、追问和决策。
- `store.js` 已经实现任务投放、完成结算、起居注、金币、精力和 59 项成就互通。
- 藏书阁上传表单只保存书名、作者和摘要，并明确提示“不实际读取文件”。
- 用户状态保存在浏览器 `localStorage(nvdi-full-v1)`。
- 项目目录约 126MB，主要由图片素材构成；尚未初始化 Git 仓库。

## 架构决定

### 1. 保留 Vanilla JS

没有迁移 React、Vue 或 Next.js。现有模块边界清晰，重写框架会增加黑客松风险，却不会直接改善核心体验。

### 2. 同源 Vercel Functions

浏览器只访问 `/api/chat` 和 `/api/knowledge/upload`。OpenAI API Key 存在 Vercel 环境变量中，不进入 HTML、JavaScript bundle 或 GitHub。

### 3. 结构化决策协议

AI 输出被约束为 `dialogue | question | decision`。`decision` 延续原有 `category / mirror / recommend / alt / tasks` 结构，因此可直接进入：

```text
presentDecision
→ applyPizhu
→ deployTasks
→ completeMapTask
→ achievement unlock
→ treasury render
```

服务端还会二次清洗分类、数值、文本长度和任务数量，避免异常模型输出破坏前端状态。

### 4. 两层知识库

- 公共知识库：维护在 `knowledge/`，由脚本预先上传，使用 `OPENAI_VECTOR_STORE_ID`。
- 用户知识库：每个浏览器首次上传时建立单独 Vector Store，后续继续复用。

服务端不直接信任客户端提交的 Vector Store ID，而是返回 HMAC 签名令牌。这样一个用户不能通过修改请求任意检索同一 OpenAI 项目下的其他知识库。

### 5. 可用性优先

真实 API 因未配置、超时、限流或上游错误不可用时，前端自动回退原有 `data.brain.analyze()`。这保证现场网络波动不会让 Demo 失去主流程。

### 6. 状态范围

黑客松版本继续使用 localStorage 保存进度。它是真实、可交互的用户数据，但只存在当前浏览器。跨设备同步需要认证与数据库，留到下一阶段。

## 文件级变更

| 文件 | 变更 |
| --- | --- |
| `api/chat.js` | 服务端 AI 对话、应用状态拼装、File Search、Structured Outputs、限流 |
| `api/knowledge/upload.js` | 文件校验、OpenAI Files 上传、Vector Store 创建与索引等待 |
| `api/_lib/*` | HTTP、OpenAI 请求、输出清洗、限流、知识库令牌签名 |
| `js/api.js` | 浏览器同源 API 适配、超时和错误标准化 |
| `js/conversation.js` | 异步真实 AI、上下文、来源展示、失败回退 |
| `js/library.js` | 真实文件选择、上传、索引状态、令牌持久化 |
| `js/store.js` | 新增 `knowledge.token` 状态和迁移兼容 |
| `css/app.css` | 决策奏折参考典籍样式 |
| `css/library.css` | 上传与知识库状态样式 |
| `knowledge/*` | 公共知识库说明和决策原则 |
| `scripts/seed-knowledge.mjs` | 公共知识库初始化脚本 |
| `tests/*` | 输出清洗与签名令牌自动测试 |
| `.env.example` | 部署环境变量模板 |
| `vercel.json` | API 30 秒执行时间及安全响应头 |
| `README.md` | 本地、知识库和 Vercel 部署说明 |

## 数据流记录

### AI 对话

1. 用户输入进入 `conversation.js`。
2. 前端采集有限状态：精力、金币、场景、未完成任务、近期起居注和藏书标题。
3. `js/api.js` POST 到 `/api/chat`。
4. 服务端验证大小和频率，验证知识库签名令牌。
5. Responses API 使用公共及用户 Vector Store 检索，并按 JSON Schema 返回结果。
6. 服务端清洗结果，前端渲染对话、追问或奏折。
7. 用户朱批同意后继续走原有任务和成就引擎。

### 典籍上传

1. 用户选择 TXT、Markdown、PDF，或把文本心得生成 TXT。
2. 浏览器上传到 `/api/knowledge/upload`。
3. 服务端新建或复用私有 Vector Store，上传 OpenAI File 并等待索引。
4. 服务端返回签名知识库令牌。
5. `store.js` 把令牌和书籍元数据保存到 localStorage。
6. 后续 `/api/chat` 验证令牌后启用该知识库。

## 安全与成本控制

- API Key 不进入前端或仓库。
- 聊天消息最长 2000 字，历史最多 12 条，输出上限 2600 tokens。
- 单服务实例默认每 10 分钟最多 24 次对话、8 次上传。
- 上传文件最大 4MB，扩展名白名单为 `.txt`、`.md`、`.pdf`。此限制为 Vercel 4.5MB Function 请求体上限预留 multipart 开销。
- 聊天和上传函数显式配置为最长 30 秒，覆盖 AI 推理和短时间索引等待。
- 模型通过 `OPENAI_MODEL` 可配置；2026-07-20 根据 OpenAI 官方最新模型解析结果，将默认值校正为 `gpt-5.6-sol`，reasoning effort 为 low。
- 正式公开前仍建议加入持久限流、验证码/登录，以及 OpenAI Project 预算限制。

## 验证记录

2026-07-20 本地验证结果：

- `npm test`：6 项通过，0 项失败。
  - 结构化决策清洗与数值裁剪。
  - 空任务安全兜底。
  - 追问选项数量限制。
  - Vector Store 令牌签名与篡改拒绝。
  - 模拟 OpenAI Responses API 的完整聊天函数请求/响应。
  - 模拟文件上传、Vector Store 创建、索引完成与签名令牌返回。
- `node --check`：`api/`、`js/`、`scripts/`、`tests/` 下全部 JavaScript/MJS 通过语法检查。
- JSON 配置：`package.json`、`vercel.json` 解析通过。
- 静态 HTTP 冒烟：`index.html`、`js/api.js`、关键中文路径场景图均返回 HTTP 200。
- 服务端无 Key 行为：`POST /api/chat` 与上传接口返回可识别的 `AI_NOT_CONFIGURED`；前端据此自动回退本地大脑。
- 方法限制：`GET /api/chat` 返回 405。

2026-07-20 线上部署记录：

- Vercel 项目：`april4xxxxs-projects/nvdi-career-decision`。
- 固定生产地址：`https://nvdi-career-decision.vercel.app`。
- 首次生产部署 ID：`dpl_8G4TYyo3aMVRjndTX4cpW3AD7RHK`；模型修正与签名密钥生效后的部署 ID：`dpl_Be7xDRqp81NK8uka6jx53Hkru49U`，状态均为 `READY`。
- 线上主页返回 HTTP 200，`/js/api.js` 返回 HTTP 200。
- 未配置 `OPENAI_API_KEY` 时，线上 `POST /api/chat` 返回 HTTP 503 和 `AI_NOT_CONFIGURED`，符合前端自动降级约定。
- 已在 Vercel Production 环境生成并保存敏感变量 `APP_SESSION_SECRET`，值未进入本地文件、命令输出或 Git。

当前未进行两项外部验证：

- 没有用户的 OpenAI API Key，因此未产生真实计费请求；通过 mock 请求验证了协议与数据流。
- 当前会话未提供可用的浏览器控制运行时，因此完成了 HTTP/代码级冒烟，没有进行截图式视觉回归。部署前仍应按 README 的验收清单人工走一遍完整页面。

## GitHub 与 Vercel 自动部署记录（2026-07-20）

- 已创建公开仓库：`https://github.com/april4xxxx/nvdi-career-decision`。
- 本地 `main` 已设置跟踪 `origin/main`，首次上传包含此前全部 4 次实现与验证提交。
- Vercel 项目已连接该 GitHub 仓库；GitHub App 仅获准访问 `nvdi-career-decision`。
- 本节提交用于触发并验证首次 GitHub → Vercel 自动部署。

## 奏折交互与返回路径修复（2026-07-20）

- 保留统一的底部大臣对话栏；只有存在待批决策时，最右侧按钮才从“探讨国事”切换为“同意”。
- 待批状态为对话组件设置受限高度：对话历史和奏折正文分别滚动，底部输入框与“同意”始终可见。
- 同意或重新分析后清除旧奏折及待批布局，避免已处理奏折继续挤占页面。
- 奏折匣的“待办奏折 / 起居注”改为明确可切换的标签按钮，并提供独立的“×”关闭按钮返回场景。
- 本地浏览器回归验证：长奏折可滚动、最右侧“同意”完整可见、备选方案正确生成备选任务、起居注可切回待办并收起抽屉。

## 本地真实 API 开发环境（2026-07-20）

- 项目已绑定 Vercel `nvdi-career-decision`，生产环境变量已拉取到被 Git 忽略的 `.env.local`。
- 原 `dev: vercel dev` 会被 Vercel CLI 当作 Development Command 再次调用，造成 recursive invocation；脚本已改名为 `local`。
- 正确启动命令：`npm run local -- --listen 127.0.0.1:3000`。本地由 Vercel Dev 同时提供静态前端和 `/api` Functions，并读取 `.env.local` 中的 DeepSeek 配置。

## AI 对话与知识检索回归修复（2026-07-20）

- 将对话发送状态与知识检索抽为 `js/conversation-rules.js` 的可测试规则。
- 待批奏折下，空输入才触发「同意」；若用户填写了再议补充，内容会发送给 AI 重新拟策。
- `dialogue` 闲聊不再把 `probed` 置为真，只有 `question` 决策追问会消耗一轮追问机会。
- 用户典籍片段继续按问题命中与入职阶段排序，但总分为 0 或负分的无关片段不再发送给 DeepSeek。
- AI 任务改为只输出预计时长；前后端使用相同固定档位计算精力、金币和恢复量，旧存档任务在加载时迁移。
- 金币结算收口到“任务确认完成”，移除朱批同意、典籍上传和心流完成的额外金币入口。
- 新增正式 SOP 编译与检索管道；库级和模块级必须同时为 `ACTIVE`，否则部署包保持空并显式标记阻断。
- 模型调用前增加阶段硬过滤与模块排除条件门，覆盖权限未开通、尚无成果两个既有 DEV 严格错误。
- 合并当前工作区的全部测试后，`npm test` 共 17 项通过。

## 重复任务与场景对话修复（2026-07-20）

- 状态版本升级至 v3，新增按场景保存的当天对话会话，内容包括消息记录、当前追问、待批决策、已选路径、输入草稿、大臣与展开状态。
- 切换宫廷地图时先保存离开场景的会话，再恢复目标场景的会话；返回原场景不再丢失此前对话或待批奏折。
- 真实跨日时，各场景旧会话先摘要归档到起居注，再开启新一天的空白会话，避免昨天的上下文误带入今天。
- 投放新任务前，对同场景未完成任务进行规范化标题相似度检查；高度重合时合并来源与知识引用，并在对话中提示已关联原奏折。
- 去重只作用于未完成任务；已完成的周期性工作之后仍可再次生成。
- 浏览器回归完成两条路径：朝堂 → 起居殿 → 朝堂后原消息与追问仍在；连续批准同一决策后奏折数保持不变，并显示“未重复创建”。
- 更新测试后，`npm test` 共 31 项通过。

## 对话收起、新建与找回交互（2026-07-20）

- 用明确的“收起”替换语义模糊的“×”；“＋ 新对话”只保留在展开态顶栏。收起态恢复最初的“大臣一句话＋常驻输入框＋探讨国事”结构，不再在摘要框里堆叠继续与新建按钮。
- 收起的待批奏折显示“继续批阅”和待批摘要，空输入点击只会恢复奏折，不会在奏折不可见时误批准。
- 开启新对话时只归档并重置当前场景；其他场景会话继续保留。归档结果可在奏折匣的起居注中查看。
- 归档时只保留用户问题与大臣的有效回复，过滤切换大臣提示、固定开场白和重复消息；若没有实质商讨则不生成起居注。
- 未发送草稿、未答追问或待批奏折存在时，先解释会失去的状态，再由用户选择继续当前对话或归档并新建。
- 宫廷地图仅为需要操作的场景显示“待答 / 待批”，普通对话不再显示标签；收起侧栏时保留对应提醒点。
- 奏折展示阶段预检同场景未完成任务；重合时在朱批前提示将关联原任务，并提供“查看原任务”。
- `Esc` 在没有弹层或抽屉时收起展开中的对话。
- 新会话主题统一使用“当前场景名＋议事”，旧存档中的“新对话”也会在恢复时自动转换。
- 任务办结弹窗收窄为专属的“任务奏报”尺寸，任务名、结算变化和操作按钮分区呈现，与奏折/宫廷视觉保持一致。
- 浏览器回归覆盖继续、收起、草稿保护、待批保护、手动归档、地图状态和任务预检；更新后 `npm test` 共 34 项通过。

## 宫廷地图滚动位置修复（2026-07-20）

- 根因是场景切换先触发全局状态刷新，随后场景对话恢复又触发一次侧栏重建；每次重建都会创建新的滚动容器并回到顶部。
- 侧栏现在持续记录自身滚动位置，点击地图前再次同步；重建完成后立即恢复，并在下一动画帧重复校正，覆盖连续刷新和布局更新。
- 浏览器回归将侧栏滚动至“钦天监 / 藏书阁 / 珍宝阁”区域后切换场景，切换完成仍保持在相同区域。

## 高倍缩放下对话区可见性修复（2026-07-20）

- 根因是六部等多任务场景的任务卡换行后占用了过多垂直空间，同时主舞台使用 `overflow: hidden`，导致底部对话框在浏览器放大后被挤出可见区并裁切。
- 应用主体与舞台的网格列改为可收缩的 `minmax(0, 1fr)`，避免内容最小宽度在窄视口下撑开页面。
- 对话区保持固定可见，剩余高度由任务区自适应；任务放不下时只在任务区内部滚动。
- 在等效 250%–300% 的极低视口中，隐藏次要场景标题、紧凑对话间距，并将奏折匣改为右上角水平胶囊，避免与对话框重叠。
- 浏览器回归覆盖等效 100%、150%、200%、250% 和 300% 缩放，对话框、输入框与发送按钮在各档位均保持在舞台可见区内。

## AI 回合的情绪稳定反馈（2026-07-20）

- 所有真实 AI 请求和本地回退生成统一进入同一个“大臣正在思考”回合容器，避免部分入口没有等待反馈。
- 生成开始后立即显示带大臣称谓的三点气泡，并向辅助技术暴露“正在思考”状态。
- 气泡至少可见 420ms，防止极快的本地结果在首帧绘制前就把它清除；结果、错误与渲染异常都通过同一收尾逻辑恢复输入。
- 生成期间同时最多存在一个思考气泡，对话历史区标记 `aria-busy`，防止用户误以为界面卡住而重复提交。

## 对外品牌名称统一（2026-07-20）

- 对外完整名称统一为“女皇入朝｜AI 职场心流决策地图”，覆盖浏览器标题、欢迎页、README、分享元数据和 AI 身份提示。
- 应用顶栏使用短名“女皇入朝”，避免完整名称挤占金币、精力和模式切换空间。
- GitHub 仓库名、npm 包名与 Vercel 固定域名保持不变，避免影响既有部署连接和评委访问地址。

## 下一阶段候选项

- Supabase Auth + 数据库，实现跨设备任务、成就、起居注同步。
- 持久化限流和验证码，保护公开 API 成本。
- 增加存档导出/导入。
- 图片 WebP/AVIF 压缩与首屏延迟加载。
- 增加真实决策样本集，对对话分类、任务拆分和数值估算做自动评测。
